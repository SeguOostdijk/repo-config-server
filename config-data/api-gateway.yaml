server:
  port: 8080
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: PRODUCT-SERVICE
          uri: http://localhost:8081
          predicates:
            - Path=/demo/**
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
